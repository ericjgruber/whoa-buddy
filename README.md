# whoa-buddy
Whoa Buddy gives a visual warning for production environments in Acquia to help prevent accidental deployments or environment changes.  Environments must be include "prod" or "production" in the environment label.   Labels with "pre prod," "pre-prod," or "preprod" will be ignored if you use a pre-production environment label.
